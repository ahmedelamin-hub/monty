/* nop.c */

#include "monty.h"

void nop(stack_t **stack, unsigned int line_number) {
    /* This function doesn't do anything. */
    (void) stack;        /* These are to avoid unused */
    (void) line_number;  /* parameter warnings. */
}

